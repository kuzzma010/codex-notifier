<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Настройки — Codex Notifier" Width="740" Height="790" MinWidth="720" MinHeight="760" WindowStartupLocation="CenterScreen" Background="{DynamicResource Surface}" Foreground="{DynamicResource Ink}" FontFamily="Segoe UI" FontSize="13">
 <Grid Margin="28,20,28,20">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <TextBlock x:Name="Eyebrow" Text="CODEX NOTIFIER  /  ПЕРВЫЙ ЗАПУСК" FontSize="11" Foreground="{DynamicResource Muted}"/>
  <StackPanel Grid.Row="1" Margin="0,22,0,24"><TextBlock Text="Настрой под себя" FontSize="26" FontWeight="Medium"/><TextBlock Text="Узнавай, когда ответ готов — удобным для тебя способом." Margin="0,7,0,0" Foreground="{DynamicResource Muted}"/></StackPanel>
  <Grid Grid.Row="2">
   <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="24"/><ColumnDefinition Width="284"/></Grid.ColumnDefinitions>
   <StackPanel>
    <TextBlock Text="ВНЕШНИЙ ВИД" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,0,0,12"/>
    <TextBlock Text="Размер уведомления" Margin="0,0,0,8"/>
    <Grid Margin="0,0,0,16"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><Button x:Name="SmallSize" Content="Маленькая" Margin="0,0,4,0"/><Button x:Name="LargeSize" Content="Большая" Grid.Column="1" Margin="4,0,0,0"/></Grid>
    <TextBlock Text="Тема" Margin="0,0,0,8"/>
    <ComboBox x:Name="ThemeChoice" Height="31" Padding="7,4"><ComboBoxItem Content="Как на устройстве"/><ComboBoxItem Content="Светлая"/><ComboBoxItem Content="Тёмная"/></ComboBox>
    <TextBlock Text="КОГДА ОТВЕТ ГОТОВ" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,24,0,14"/>
    <CheckBox x:Name="SoundChoice" Content="Звуковой сигнал" Margin="0,0,0,15"/>
    <CheckBox x:Name="AutoChoice" Content="Открывать чат автоматически"/>
    <TextBlock Text="Если уведомление осталось без ответа" Foreground="{DynamicResource Muted}" FontSize="11" Margin="0,5,0,10"/>
    <StackPanel Orientation="Horizontal"><TextBlock Text="Через" VerticalAlignment="Center" Margin="0,0,10,0"/><ComboBox x:Name="DelayChoice" Width="115" Height="29" Padding="7,3"><ComboBoxItem Content="15 секунд"/><ComboBoxItem Content="30 секунд"/><ComboBoxItem Content="1 минуту"/></ComboBox></StackPanel>
    <TextBlock Text="БЫСТРЫЕ ОТВЕТЫ" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,20,0,10"/>
    <ComboBox x:Name="ReplyChoice" Height="31"><ComboBoxItem Content="Вставлять для проверки"/><ComboBoxItem Content="Отправлять сразу"/></ComboBox>
    <TextBlock Text="Да · Дальше · Делай — в большой карточке" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,6,0,0"/>
    <TextBlock Text="ЗАПУСК" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,24,0,14"/>
    <CheckBox x:Name="StartupChoice" Content="Запускать вместе с Windows"/>
   </StackPanel>
   <Border Grid.Column="2" Background="{DynamicResource PreviewBackground}" CornerRadius="16" Padding="16" VerticalAlignment="Top" MinHeight="290">
    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <TextBlock Text="ПРЕДПРОСМОТР" FontSize="11" Foreground="{DynamicResource Muted}"/>
     <Border x:Name="PreviewShell" Grid.Row="1" Background="{DynamicResource Glass}" BorderBrush="{DynamicResource GlassLine}" BorderThickness="1" CornerRadius="16" Padding="6" Margin="0,38,0,32" HorizontalAlignment="Right">
      <Grid><Grid x:Name="PreviewMini"><Grid.ColumnDefinitions><ColumnDefinition Width="95"/><ColumnDefinition Width="34"/></Grid.ColumnDefinitions><Button x:Name="PreviewMiniOpen" Content="Открыть" Style="{DynamicResource Primary}"/><Button x:Name="PreviewExpand" Grid.Column="1" Content="⋯" Padding="4" BorderThickness="0" Background="Transparent" AutomationProperties.Name="Развернуть превью"/></Grid>
       <StackPanel x:Name="PreviewLarge" Width="216" Visibility="Collapsed"><DockPanel><Button x:Name="PreviewCollapse" Content="⌄" DockPanel.Dock="Right" Background="Transparent" BorderThickness="0" Padding="4,0" AutomationProperties.Name="Свернуть превью"/><TextBlock Text="✓  Codex" FontSize="11" Foreground="{DynamicResource Muted}"/></DockPanel><TextBlock Text="Ответ готов" FontSize="18" FontWeight="Medium" Margin="0,14,0,6"/><TextBlock Text="Визуальный стиль уведомлений" FontSize="11" Foreground="{DynamicResource Muted}"/><TextBlock x:Name="PreviewCountdown" FontSize="11" Margin="0,16,0,8" Foreground="{DynamicResource Muted}"/><Border x:Name="PreviewProgress" Height="3" Background="{DynamicResource Accent}" CornerRadius="2"/><Grid Margin="0,14,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><Button x:Name="PreviewOpen" Content="Открыть чат" Style="{DynamicResource Primary}" FontSize="11" Padding="5,8" Margin="0,0,3,0"/><Button x:Name="PreviewAck" Content="Увидел" Grid.Column="1" FontSize="11" Padding="5,8" Margin="3,0,0,0"/></Grid><UniformGrid Columns="3" Margin="0,10,0,0"><Button x:Name="PreviewYes" Content="Да" FontSize="11" Padding="4,6" Margin="0,0,3,0"/><Button x:Name="PreviewNext" Content="Дальше" FontSize="11" Padding="4,6" Margin="0,0,3,0"/><Button x:Name="PreviewDo" Content="Делай" FontSize="11" Padding="4,6"/></UniformGrid></StackPanel>
      </Grid>
     </Border>
     <StackPanel Grid.Row="2"><TextBlock x:Name="PreviewStatus" Text="Изменения сразу видны здесь" FontSize="11" TextWrapping="Wrap" Foreground="{DynamicResource Muted}" Margin="0,0,0,10"/><Button x:Name="TestNotice" Content="Проверить уведомление"/></StackPanel>
    </Grid>
   </Border>
  </Grid>
  <Grid Grid.Row="3" Margin="0,22,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="Настройки доступны через значок рядом с часами." TextWrapping="Wrap" Width="280" HorizontalAlignment="Left" Foreground="{DynamicResource Muted}" FontSize="11" VerticalAlignment="Center"/><Button x:Name="SaveSettings" Content="Сохранить и начать" Grid.Column="1" Style="{DynamicResource Primary}" Padding="18,11"/></Grid>
 </Grid>
</Window>

