<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Settings — Codex Notifier" Width="740" Height="790" MinWidth="720" MinHeight="760" WindowStartupLocation="CenterScreen" Background="{DynamicResource Surface}" Foreground="{DynamicResource Ink}" FontFamily="Segoe UI" FontSize="13">
 <Grid Margin="28,20,28,20">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <TextBlock x:Name="Eyebrow" Text="CODEX NOTIFIER  /  FIRST RUN" FontSize="11" Foreground="{DynamicResource Muted}"/>
  <StackPanel Grid.Row="1" Margin="0,22,0,24"><TextBlock Text="Make it yours" FontSize="26" FontWeight="Medium"/><TextBlock Text="Know when a reply is ready, your way." Margin="0,7,0,0" Foreground="{DynamicResource Muted}"/></StackPanel>
  <Grid Grid.Row="2">
   <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="24"/><ColumnDefinition Width="284"/></Grid.ColumnDefinitions>
   <StackPanel>
    <TextBlock Text="APPEARANCE" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,0,0,12"/>
    <TextBlock Text="Notification size" Margin="0,0,0,8"/>
    <Grid Margin="0,0,0,16"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><Button x:Name="SmallSize" Content="Small" Margin="0,0,4,0"/><Button x:Name="LargeSize" Content="Large" Grid.Column="1" Margin="4,0,0,0"/></Grid>
    <TextBlock Text="Theme" Margin="0,0,0,8"/>
    <ComboBox x:Name="ThemeChoice" Height="31" Padding="7,4"><ComboBoxItem Content="Follow system"/><ComboBoxItem Content="Light"/><ComboBoxItem Content="Dark"/></ComboBox>
    <TextBlock Text="WHEN A REPLY IS READY" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,24,0,14"/>
    <CheckBox x:Name="SoundChoice" Content="Play a sound" Margin="0,0,0,15"/>
    <CheckBox x:Name="AutoChoice" Content="Open task automatically"/>
    <TextBlock Text="If the notification has not been acknowledged" Foreground="{DynamicResource Muted}" FontSize="11" Margin="0,5,0,10"/>
    <StackPanel Orientation="Horizontal"><TextBlock Text="After" VerticalAlignment="Center" Margin="0,0,10,0"/><ComboBox x:Name="DelayChoice" Width="115" Height="29" Padding="7,3"><ComboBoxItem Content="15 seconds"/><ComboBoxItem Content="30 seconds"/><ComboBoxItem Content="1 minute"/></ComboBox></StackPanel>
    <TextBlock Text="QUICK REPLIES" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,20,0,10"/>
    <ComboBox x:Name="ReplyChoice" Height="31"><ComboBoxItem Content="Insert for review"/><ComboBoxItem Content="Send immediately"/></ComboBox>
    <TextBlock Text="Yes · Continue · Do it — in the expanded card" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,6,0,0"/>
    <TextBlock Text="STARTUP" FontSize="11" Foreground="{DynamicResource Muted}" Margin="0,24,0,14"/>
    <CheckBox x:Name="StartupChoice" Content="Launch with Windows"/>
   </StackPanel>
   <Border Grid.Column="2" Background="{DynamicResource PreviewBackground}" CornerRadius="16" Padding="16" VerticalAlignment="Top" MinHeight="290">
    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <TextBlock Text="PREVIEW" FontSize="11" Foreground="{DynamicResource Muted}"/>
     <Border x:Name="PreviewShell" Grid.Row="1" Background="{DynamicResource Glass}" BorderBrush="{DynamicResource GlassLine}" BorderThickness="1" CornerRadius="16" Padding="6" Margin="0,38,0,32" HorizontalAlignment="Right">
      <Grid><Grid x:Name="PreviewMini"><Grid.ColumnDefinitions><ColumnDefinition Width="95"/><ColumnDefinition Width="34"/></Grid.ColumnDefinitions><Button x:Name="PreviewMiniOpen" Content="Open" Style="{DynamicResource Primary}"/><Button x:Name="PreviewExpand" Grid.Column="1" Content="⋯" Padding="4" BorderThickness="0" Background="Transparent" AutomationProperties.Name="Expand preview"/></Grid>
       <StackPanel x:Name="PreviewLarge" Width="216" Visibility="Collapsed"><DockPanel><Button x:Name="PreviewCollapse" Content="⌄" DockPanel.Dock="Right" Background="Transparent" BorderThickness="0" Padding="4,0" AutomationProperties.Name="Collapse preview"/><TextBlock Text="✓  Codex" FontSize="11" Foreground="{DynamicResource Muted}"/></DockPanel><TextBlock Text="Reply ready" FontSize="18" FontWeight="Medium" Margin="0,14,0,6"/><TextBlock Text="Notification appearance" FontSize="11" Foreground="{DynamicResource Muted}"/><TextBlock x:Name="PreviewCountdown" FontSize="11" Margin="0,16,0,8" Foreground="{DynamicResource Muted}"/><Border x:Name="PreviewProgress" Height="3" Background="{DynamicResource Accent}" CornerRadius="2"/><Grid Margin="0,14,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><Button x:Name="PreviewOpen" Content="Open task" Style="{DynamicResource Primary}" FontSize="11" Padding="5,8" Margin="0,0,3,0"/><Button x:Name="PreviewAck" Content="Got it" Grid.Column="1" FontSize="11" Padding="5,8" Margin="3,0,0,0"/></Grid><UniformGrid Columns="3" Margin="0,10,0,0"><Button x:Name="PreviewYes" Content="Yes" FontSize="11" Padding="4,6" Margin="0,0,3,0"/><Button x:Name="PreviewNext" Content="Continue" FontSize="11" Padding="4,6" Margin="0,0,3,0"/><Button x:Name="PreviewDo" Content="Do it" FontSize="11" Padding="4,6"/></UniformGrid></StackPanel>
      </Grid>
     </Border>
     <StackPanel Grid.Row="2"><TextBlock x:Name="PreviewStatus" Text="Your changes appear here" FontSize="11" TextWrapping="Wrap" Foreground="{DynamicResource Muted}" Margin="0,0,0,10"/><Button x:Name="TestNotice" Content="Test notification"/></StackPanel>
    </Grid>
   </Border>
  </Grid>
  <Grid Grid.Row="3" Margin="0,22,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="Open settings from the tray icon next to the clock." TextWrapping="Wrap" Width="280" HorizontalAlignment="Left" Foreground="{DynamicResource Muted}" FontSize="11" VerticalAlignment="Center"/><Button x:Name="SaveSettings" Content="Save and start" Grid.Column="1" Style="{DynamicResource Primary}" Padding="18,11"/></Grid>
 </Grid>
</Window>

